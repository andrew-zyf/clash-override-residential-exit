# Changelog

版本号对应脚本头部的 `@version`。

---

## v12.0 (2026-05-09)

- 项目名更新为 `clash-override-miya-residential-exit`。
- 覆写脚本改名为 `src/miya-residential-exit-override.js`。
- 运行时代码改为 MiyaIP 家宽出口语义：核心出口组为 `az.核心出口.🏠 家宽出口`，只包含 `MiyaIP（官方中转）`。
- 用户配置改为 `routeBrowserToResidentialExit`。
- 文档重写为当前架构说明，并移除全部图片引用；架构图改用 Mermaid。
- 删除仓库内 PNG 配图资源。
